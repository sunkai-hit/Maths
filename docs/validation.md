# 验证记录与边界

## 原始构建验证

执行 `node work/validate.mjs`：检查六章、26 个教材节次及六个复习入口有内容，题型与练习 ID 唯一，每题型有三个难度层级，题目具有答案和解析，选择题四选项和答案标记合法，示意图引用点存在，网页引用文件存在。

数值抽查包括三边关系的整数计数、勾股定理边长与面积、打印店计费方案在 1～100 页的比较。它们不等于对所有题目数学推理进行了自动证明。

执行 `node work/check-offline.mjs`：离线 HTML 内含 10 段脚本（含新增 PDF 导出器），脚本语法通过，无外部样式或脚本依赖，包含打印功能。

浏览器检查过：题库加载、题型入口、选题、练习卷设置、学生卷预览不出现练习答案、家长卷预览包含答案与步骤、界面日志无错误。WebMCP 接口可加入有效题号、返回选题，拒绝无效题号。检查过手机宽度下的节次选择器与页面宽度。

## 归档导出验证

`scripts/export-data.mjs` 从应用数据文件生成 JSON 与 SQL。SQL 用 Python 标准库 sqlite3 导入内存数据库后检查完整性、外键、章节数、节次数、题型数、例题数和练习数。

GitHub 上传以文件清单中的 Git blob SHA-1 与远端树逐项比较，确认源文件及归档没有在传输时截断或改写。上传清单自身不纳入自身哈希，避免循环依赖。

## 未完成的质量保障

- 全题库独立教研审核及难度标定。
- 全部可能的打印纸张、浏览器、实体打印机实测；当前已检查 PDF 实际下载文件及渲染，但未进行实体打印机出纸测试。
- 所有变式、综合考法的完备性证明。
- 后端数据库与多用户同步：目前未实现，也不是第一版已确认范围。

## 2026-09-12：打印与 PDF 修复验证

原按钮只调用浏览器打印接口，当前应用内浏览器未显示可用打印窗口。增加独立 PDF 导出后，在同一浏览器中实际点击下载，确认下载目录生成文件；下载事件监听曾超时，但磁盘文件与页面导出内容 SHA-256 一致，下载本身成功。

- 学生卷：两道原选题，1 页 A4，无练习答案，下载文件 133205 字节。
- 答案卷：临时加入一道带图形的练习并附两道例题，2 页 A4，包含例题、答案和完整解析，文件 527906 字节。
- 使用 pypdf 严格模式解析两份 PDF；页面均为 595.28 × 841.89 pt。用 Poppler 渲染并逐页查看，确认中文、三角形及点标记、题干、答案和页码显示正常。
- 从 PDF 预览调用原生打印后，三个练习正确进入专用打印容器，弹窗恢复，页面退出打印状态。宿主浏览器仍可能不弹出系统打印窗口；界面提示下载 PDF 后打印。
- 验证练习卷底部一键生成并下载 PDF；测试后移除临时题目，恢复原两道题、学生卷和不附例题设置。
- `node scripts/test-pdf.mjs`：当时全册 147 题 × 学生/答案 × 附例题开关 × 三档留白，共 12 组布局，加 1 组超长内容；检查每题恰好出现一次、答案隔离、图形数量、页边界、内容无重叠，以及 PDF 二进制 stream 长度和 xref 偏移。
- 题库验证与离线脚本检查通过。当前命令环境未提供 npm，直接执行 package.json 中的三个 Node 检查脚本，全部通过。

PDF 使用高清图像页，不能选中检索文字；可复制文字版在预览下方。自动布局测试采用确定性的字符宽度估算，不能替代所有系统字体和浏览器的视觉检查。

## 2026-09-12：第一章完整材料扩充

依据《鲁教七上数学（1三角形）》完整材料，将第一章由原 7 个核心题型扩充为 22 个题型，共 22 道讲解例题和 66 道分层练习；覆盖 1.1～1.4 正课、尺规作图、利用全等测距离，以及角平分线与高线、常见全等模型、倍长中线、截长补短、作平行线、半角模型等专题。

- 保留原核心 ID `t01`～`t07`，新增题型固定为 `c1-01`～`c1-15`；第二章及以后仍从 `t08` 延续，避免历史浏览器草稿中的后续题号整体漂移。
- 已对 `chapter1a.js`～`chapter1d.js` 执行 Node 语法检查，并以独立 VM 校验第一章 22 个题型、66 道练习、ID 唯一、每题三档难度、选择题四选项与答案标记等结构规则，检查通过。
- 对答案区逐题回看，尤其核对了全等性质、SSS/ASA/AAS/SAS、综合应用、测距和专题辅助线题；依赖原图的题目补充了必要的位置关系文字，避免脱离原图后题意不完整。
- `dist/index.html`、`scripts/export-data.mjs`、`work/validate.mjs`、`scripts/test-pdf.mjs` 已同步加入四个新的第一章题库脚本；PDF 测试中的题量断言已改为动态计数。
- 当前 GitHub 中 `dist/` 为最新运行题库。`data/question-bank.json`、`database/question-bank.sql` 与 `outputs/循题-七上数学家长组卷.html` 属于生成物，本次连接环境不能在仓库工作区直接执行 Node 构建，因此它们仍是上一次生成快照；在本地 Node 20+ 环境运行 `npm run export:data`、`npm run build:offline` 后即可同步刷新。

本次难度标签用于适配现有“基础/提高/压轴”筛选，并非原材料给出的官方难度标定。对手写/扫描解析中有符号错位的内容只做数字化表述和符号统一，不另行改写知识结论。
